apply plugin: "java"

version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

compileJava {
    // options.compilerArgs << "-Astrict=true"
}

dependencies {
    compileOnly project(':ladon-core')
    annotationProcessor project(':ladon-core')
}
